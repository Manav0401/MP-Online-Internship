# Saved Model

This directory stores the trained model.

After training, the model will be saved as:

```text
resnet50_best.keras
```

The trained model is not included in this repository because it exceeds GitHub's file size limit.